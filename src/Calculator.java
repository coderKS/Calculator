package src;

import java.math.BigDecimal;

public class Calculator {
	public Double calculate(String expr) {
		if(!expr.isEmpty()){
			return new Double(1);
		}
		return new Double(0);
	}
	
}
